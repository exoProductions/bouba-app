import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.bouba.production',
  appName: 'Bouba',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
